{% for key,value in filter_item.items %}
	{# <div class="filter_check {{filter_item.active[key] is defined ? '_checked':''}}" data-type="{{label}}" data-filter-checkbox-item>
		<div class="filter_check_item" data-value="{{key}}"><p>{{value}}</p></div>
	</div> #}

	<label class="filter_check {{filter_item.active[key] is defined ? '_checked':''}}">
		<input type="radio" name="filter-price" data-value="{{key}}" data-filter-checkbox-item>
		<span>{{value}}</span>
	</label>
{% endfor %}
